---
import Layout from "../layouts/Layout.astro";
import Breadcrumbs from "../components/ui/Breadcrumbs.astro"
import FaqItem from "../components/features/faq/FaqItem.astro"
import Button from "../components/ui/Button.astro"
import { get } from "../lib/strapi";

const faqs = (await get("faqs")) || [];

const data = faqs.length > 0 ? faqs : [
  {
    question: "¿De dónde proviene vuestra miel?",
    answer: "Nuestra miel proviene de colmenas situadas en entornos naturales protegidos de la Península Ibérica. Trabajamos directamente con apicultores locales que practican una apicultura sostenible y respetuosa con las abejas."
  }
];
---

<Layout
  titulo="Preguntas Frecuentes (FAQ)"
  descripcion="Resuelve tus dudas sobre nuestros productos, envíos y devoluciones en nuestra sección de Preguntas Frecuentes."
  keywords="faq miel, preguntas frecuentes, dudas compra miel, origen miel"
>
  <section class="py-12 bg-bg-primary border-b border-divider">
    <div class="container mx-auto px-4 lg:px-8">
      <Breadcrumbs separator="/" homeLabel="Inicio" />
      <h1 class="text-4xl font-bold text-text-primary mb-2">Preguntas Frecuentes</h1>
      <p class="text-lg text-text-secondary max-w-2xl">
        Aquí encontrarás respuestas a las dudas más comunes sobre nuestros productos y servicios.
      </p>
    </div>
  </section>

  <section class="py-16 bg-bg-secondary">
    <div class="container mx-auto px-4 lg:px-8">
      <div class="max-w-3xl mx-auto space-y-4">

        {data.map((faq: any) => (
            <FaqItem question={faq.question} answer={faq.answer} />
        ))}

      </div>

      <div class="text-center mt-12">
          <p class="text-text-secondary mb-4">¿No encuentras lo que buscas?</p>
          <Button link="/contacto" variant="primary">
              Contáctanos
          </Button>
      </div>
    </div>
  </section>
</Layout>

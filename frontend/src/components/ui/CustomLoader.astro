---
import { get } from '../../lib/strapi';

interface Props {
  size?: number;
  color?: string;
  className?: string;
}

const { size = 40, color = "currentColor", className = "" } = Astro.props;

const customLoaderIcon = await get('custom-loader-icon');

console.log(customLoaderIcon);
---

<div
  class:list={["loader-container", className]}
  role="status"
  aria-label="Cargando"
  style={`--loader-size: ${size}px; --loader-color: ${color};`}
>
  <svg
    class="loader-svg"
    viewBox="0 0 24 24"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
  >
    <circle
      class="opacity-25"
      cx="12"
      cy="12"
      r="10"
      stroke="currentColor"
      stroke-width="4"></circle>
    <path
      class="opacity-75"
      fill="currentColor"
      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
    ></path>
  </svg>
  <span class="sr-only">Cargando...</span>
</div>

<style>
  .loader-container {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: var(--loader-size);
    height: var(--loader-size);
    color: var(--loader-color);
  }

  .loader-svg {
    animation: spin 1s linear infinite;
    width: 100%;
    height: 100%;
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }
</style>
